---
name: nafura-orch
description: Paused. Do not start Raster sessions, inbox, or Spec→Code harness.
---

# Orchestrator — pause Raster

**Ne pas lancer Raster** (`inbox`, `window`, `ready`, `run`, harness Spec → Code).

Traiter la demande dans cette session : discuter, puis implémenter. Pas de sous-sessions Raster.
Sektor locale si UI/API : `make -C nafura-platform/ops mode-b`.
