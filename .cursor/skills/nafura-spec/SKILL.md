---
name: nafura-spec
description: Paused. Do not plan via Raster lots, 00-PLAN, or t.mjs Tasks. Discuss and implement in sources/.
---

# Spec — pause Raster

**Ne pas utiliser ce skill comme pipeline Raster.**

Pas de `00-PLAN.md` obligatoire, pas de `t.mjs new`, pas de sous-lot, pas de Pact.

Clarifier dans le chat, wireframe canvas si UI, puis coder dans `<projet>/sources/`.
QA Sektor : `.cursor/rules/cursor-qa-browser.mdc` (`make -C nafura-platform/ops mode-b`).
