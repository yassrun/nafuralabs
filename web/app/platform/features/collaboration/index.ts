/**
 * Platform collaboration features: notification, doc-manager, audit, comment, workflow, tagging.
 * Import from subpaths to avoid type conflicts, e.g. @services/collaboration/notification
 */
// Do not export * from all submodules (PageResponse etc. would conflict). Use direct subpath imports.
