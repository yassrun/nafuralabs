apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: corporate

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: corporate-web
    newName: corporate-web
    newTag: staging
