---
name: orchestration
description: Paused. Do not start Raster sessions or the Spec → Code pipeline.
---

# Orchestration — pause Raster

**Ne pas utiliser Raster** (`t.mjs window/ready/run/check`, lots, Tasks).

Rester dans la session courante. Coder dans `sources/`.
Sektor locale : `make -C nafura-platform/ops mode-b` (`.cursor/rules/cursor-qa-browser.mdc`).
