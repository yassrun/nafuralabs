-- Lot 8 — suppression tables consultation (après retrait du code Java / entités JPA)
-- Ordre imposé par les FK : composants → noeuds → consultations

DROP TABLE IF EXISTS consultation_composants;
DROP TABLE IF EXISTS consultation_noeuds;
DROP TABLE IF EXISTS consultations;
