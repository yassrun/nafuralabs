# Archive epics

Epics hors vague live (historique Markdown).

**Aucun epic live Sektor** dans `epics/` hors `qa-local-auth-seed/` (spec only) — le chantier
référentiel / catalogue est archivé ici :
[`referentiel-catalogue-sektor/`](referentiel-catalogue-sektor/).

Les tickets PM associés restent dans `pm/` ou `pm/backlog_archive/`.
