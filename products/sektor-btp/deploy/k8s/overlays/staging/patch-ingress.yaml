apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: erp-ingress
spec:
  rules:
    - host: api.staging.sektor.nafuralabs.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sektor-btp-backend
                port:
                  number: 8082
    - host: staging.sektor.nafuralabs.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sektor-btp-web
                port:
                  number: 80
