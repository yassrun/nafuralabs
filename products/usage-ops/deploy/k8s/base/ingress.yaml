apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: usage-ops
spec:
  ingressClassName: nginx
  rules:
    - host: usage-ops.nafura.local
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: usage-ops-backend
                port:
                  number: 8087
          - path: /
            pathType: Prefix
            backend:
              service:
                name: usage-ops-web
                port:
                  number: 80
