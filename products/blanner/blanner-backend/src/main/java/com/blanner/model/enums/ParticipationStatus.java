package com.blanner.model.enums;

public enum ParticipationStatus {
    NOT_REQUESTED,  // ← BEFORE clicking "I'm in"
    REQUESTED,
    SHORTLISTED,
    ACCEPTED,
    REJECTED,
    CANCELED,
    LEFT
}
