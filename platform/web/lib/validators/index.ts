/**
 * Validateurs purs partagés — appartiennent à la PLATEFORME.
 *
 * Validateurs marocains (ICE, RIB, CNSS, IF, patente…) consommés par les atomes
 * du design system (`ice-input`, `rib-input`, `phone-ma-input`). Ancrage MA assumé
 * tant qu'un produit non marocain n'apparaît pas.
 */
export * from './ma-validators';
