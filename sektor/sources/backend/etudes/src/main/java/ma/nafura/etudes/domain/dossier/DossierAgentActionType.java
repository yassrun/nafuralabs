package ma.nafura.etudes.domain.dossier;

/** Geste métier borné du panneau agent dossier (SEKTOR-218 AC-16). */
public enum DossierAgentActionType {
    CHIFFRAGE,
    INCOHERENCES,
    RATTACHEMENTS_CATALOGUE
}
