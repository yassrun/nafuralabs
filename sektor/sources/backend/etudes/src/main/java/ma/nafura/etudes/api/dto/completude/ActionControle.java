package ma.nafura.etudes.api.dto.completude;

/** Action de résolution suggérée pour un contrôle (SEKTOR-202 read model). */
public enum ActionControle {
    DEPOSER_PIECE,
    CHOISIR_VOIE,
    CORRIGER_ARBRE,
    CHIFFRER_POSTE,
    TRAITER_BLOCAGE,
    DEMANDER_VALIDATION,
    GENERER_DEVIS,
    CONSULTER
}
