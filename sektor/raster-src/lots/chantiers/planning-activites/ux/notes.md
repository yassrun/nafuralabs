# UX — workspace planning conducteur

SSOT canvas : `planning-workspace-wireframe.canvas.tsx` (copie preview `canvases/` même nom).

## Décisions

- Qualité = drawer étude (chiffrage), pas Gantt MSP.
- Lots hors calendrier ; rattachement dans le drawer.
- CTA + empty state ; jamais « via l'API ».
- Pas d'IA dans ce contrat.

## États

vide · Gantt avec activités · drawer création · drawer rattachement / avancement · refus quotité.
