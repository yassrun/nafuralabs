# Raster — UI (legacy)

L'app Raster vit dans le **projet** [`raster/`](../../raster/). Ce dossier ne contient plus que le front (`web/`),
à déplacer vers `raster/web/` — c'est un `TECHNICAL` à ouvrir.

| Quoi | Où |
|------|-----|
| Moteur, contrat, vues | [`raster/`](../../raster/) |
| Tickets | `<projet>/raster-src/lots/…/tasks/` |
| Inbox | `raster/inbox.md` |
| Canvas | `raster/raster-src/lots/<lot>/<sous-lot>/ux/*.canvas.tsx` |
