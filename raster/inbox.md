# INBOX

<!-- Capture globale Raster — une ligne, @tag optionnel, pas d'ID. -->
<!-- Promote → <projet>/raster-src/lots/<lot>/<sous-lot?>/tasks/ -->

- some test of capture
- @ops image `nafura-lifecycle:staging` stale : changelogs chantiers v1.10/v1.11 absents du job migrate Mode B (SQL appliqué à la main pour SEKTOR-328)
- @chantiers `jour_semaine` SMALLINT vs JPA `int` — Hibernate validate échoue tant que la colonne n’est pas INTEGER
- @rh boot Mode B : `employes.departement_id` + `RhNomenclatureCodes` public requis par `QaLocalEmployeProvisioner` (voisin SEKTOR-331)
