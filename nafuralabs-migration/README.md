# nafuralabs-migration

Projet **Raster-only** : programme strangler du workspace (`NAFURALABS.md`).

Pas de `pact/`, pas d’`ops/`. Les SPEC s’écrivent sur `nafura-platform` et `sektor`.

Tickets : `raster-src/lots/strangler/` — préfixe `MIG-*`.
